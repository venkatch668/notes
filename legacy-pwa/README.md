# Legacy PWA (superseded)

The original zero-build React-via-CDN prototype that lived at the repo root.
Kept only because it had uncommitted local changes when the app was replaced by
`frontend/` (React + TypeScript + Vite + Bootstrap).

Nothing references these files. Delete this folder once you are happy with the
new app.
